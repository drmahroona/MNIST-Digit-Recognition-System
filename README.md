# MNIST-Digit-Recognition-System
This project is an interactive MNIST digit recognition system built with Streamlit and PyTorch. It provides a user-friendly interface to draw handwritten digits or upload images, and uses a CNN trained on the MNIST dataset to predict the digit with high accuracy.
